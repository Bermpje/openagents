network:
  name: "Event Logging Demo"
  mode: "centralized"
  host: "0.0.0.0"
  port: 8700

  # 不需要密码认证
  default_agent_group: "guest"
  requires_password: false

  transports:
    - type: "http"
      config:
        host: "0.0.0.0"
        port: 8700

  mods: []
